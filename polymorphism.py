name = "Krish"
print(len(name))

list = [1, 2]
print(len(list))

#polymorphism means many forms
#example is len function as it is a single entity and it knows how to
#handle different datatypes or objects